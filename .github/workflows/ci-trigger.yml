name: Midnight Oracle CI Trigger

on:
  push:
    branches:
      - main
      - 'rebuild/**'
  pull_request:
    branches:
      - main
      - 'rebuild/**'

permissions:
  contents: read

jobs:
  trigger:
    uses: ./.github/workflows/ci.yml
